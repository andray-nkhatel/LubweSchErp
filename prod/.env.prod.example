# .env.prod.example

# PostgreSQL Database
POSTGRES_DB=your_database_name
POSTGRES_USER=your_database_user
POSTGRES_PASSWORD=your_database_password

# Backend (ASP.NET Core)
# This should be a strong, complex key for JWT token generation/validation
JWT_KEY=your_jwt_secret_key_very_long_and_complex

# Frontend (Vue.js)
# URL of your backend API. This will be used during the frontend build process.
# VITE_API_URL=https://api.yourdomain.com
